@model IEnumerable<PAKDial.Domains.UserEndViewModel.CompanyListingContact>
@if (Model.Count() > 0)
{
    <div>
        <div class="calpop1">
            <div class="calwpr1 dt">
                <div class="tbcalWpr">
                    <span class="tbcaltxt">Tap to Call</span>
                </div>
                <ul class="calnbrwp">
                    @foreach (var item in Model)
                    {
                        <li>@item.Number</li>
                    }

                </ul>
            </div>
            <span class="cancelbtn dt" id="btn-modal-cancel" onclick="Hide()">Cancel</span>
        </div>
    </div>
}






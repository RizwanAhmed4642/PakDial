   <!-- AdminBar -->
@await Component.InvokeAsync("AdministratorBarComponent")
<!-- End AdminBar -->
<div class="header-top pd-primary-bg">
    <div class="container">
        <div class="row">
            <div class="col-lg-6 col-md-4 col-5">
                <ul class="top-contact">
                    <li>
                        <a href="#" class="pd-secondary-font"><i class="fa fa-envelope"></i>info@pakdial.com</a>
                    </li>
                </ul>
            </div>
            <div class="col-lg-6 col-md-8 col-7">
                <ul class="nav justify-content-end top-links">
                    <li class="nav-item">
                        @if (User.Identity.IsAuthenticated)
                        {
                            <a class="nav-link head-about pd-box-shadow" onclick="location.href='/ClientListing/AddClientCompanyListing'" id="ShowFreeListingForm">Add Listing</a>
                        }
                        else
                        {
                            <a class="nav-link head-about pd-box-shadow" onclick="location.href='/Home/Register'" id="ShowFreeListingForm">List Your Business</a>
                        }
                    </li>
                    @if (User.Identity.IsAuthenticated)
                    {
                        @await Component.InvokeAsync("CustomerProfileShowComponent")

                    }
                    else
                    {
                        <li class="nav-item">
                            @*id="ClientLoginBtn"*@
                            <a class="nav-link pd-secondary-font" id="ClientLoginsBtn"><i class="fa fa-user"></i>Login</a>
                    </li>    
                    <li class="nav-item">
                            <a class="nav-link pd-secondary-font" id="ClientSignUpBtn"><i class="fa fa-user-plus"></i> Sign Up</a>

                        </li>
                        }
                    </ul>
            </div>
        </div>
    </div>
</div>

<!--Popup -->
@*<partial name="_ClientLoginOtpLogin">
    <partial name="_ClientOtpCodeOtp">*@
@*<partial name="_ClientLoginLogin">*@
@*@await Html.PartialAsync("~/Views/Shared/_ClientLoginLogin.cshtml")
@await Html.PartialAsync("~/Views/Shared/_ClientForgotPassword.cshtml")*@
<!-- End Popup -->
